describe('Julietest', () => {

	it('Should giveTake', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="giveCount"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('1')
		cy.get('[data-test="giveButton"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="takeCount"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('1')
		cy.get('[data-test="takePaid"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('750')
		cy.get('[data-test="takeButton"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productBack"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should receiveBox', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="menuReceive"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="receiveSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="receiveListCount"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('5')
		cy.get('[data-test="receiveSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('PDM')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="receiveListCount"]:eq(' + ('1' ? '1' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('5')
		cy.get('[data-test="receiveButton"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should sellBox', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="menuSell"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="sellSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="sellListCount"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('5')
		cy.get('[data-test="sellSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('PDM')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="sellListCount"]:eq(' + ('1' ? '1' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('5')
		cy.get('[data-test="sellPaid"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('7500')
		cy.get('[data-test="sellButton"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should loan', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="menuLoan"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="loanPaid"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('500')
		cy.get('[data-test="loanButton"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should menuDesign', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should storageSearch', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
	})
	it('Should storageTakeOne', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
		cy.get('[data-test="productTakeOne"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productGiveOne"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should newProduct', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="menuReceive"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="receiveSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('LiftOff')
		cy.get('[data-test="productNotFound"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productNumber"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('1142857')
		cy.get('[data-test="productName"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('Liftoff')
		cy.get('[data-test="productPrice"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('750')
		cy.get('[data-test="productCreate"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should modifyProduct', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productModify"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productSave"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should intLogin', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should intNewProduct', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="menuReceive"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="receiveSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('LiftOff')
		cy.get('[data-test="productNotFound"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productNumber"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('1142857')
		cy.get('[data-test="productName"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('Liftoff')
		cy.get('[data-test="productPrice"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('750')
		cy.get('[data-test="productCount"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('5')
		cy.get('[data-test="productCreate"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should intReceiveBox', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="menuReceive"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="receiveSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="receiveListCount"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('5')
		cy.get('[data-test="receiveSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('PDM')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="receiveListCount"]:eq(' + ('1' ? '1' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('5')
		cy.get('[data-test="receiveButton"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should intSellBox', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="menuSell"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="sellSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="sellListCount"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('5')
		cy.get('[data-test="sellSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('PDM')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="sellListCount"]:eq(' + ('1' ? '1' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('5')
		cy.get('[data-test="sellPaid"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('7500')
		cy.get('[data-test="sellButton"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should intStorageTakeOne', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
		cy.get('[data-test="productTakeOne"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should intStorageGiveOne', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
		cy.get('[data-test="productGiveOne"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should intNewProduct', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="menuReceive"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="receiveSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('LiftOff')
		cy.get('[data-test="productNotFound"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productNumber"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('1142857')
		cy.get('[data-test="productName"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('Liftoff')
		cy.get('[data-test="productPrice"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('750')
		cy.get('[data-test="productCount"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('5')
		cy.get('[data-test="productCreate"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should intModifyProduct', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productSearch"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('F1')
		cy.get('[data-test="productItem"]:eq(' + ('0' ? '0' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productModify"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="productSave"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should transactions', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="loginUsername"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="loginPassword"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('mojeMilaJulinka')
		cy.get('[data-test="loginSend"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="menuLoan"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})

})